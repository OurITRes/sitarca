// Central application configuration defaults
export const APP_CONFIG = {
  appName: 'CyberWatch',
  appSuffix: '.AI',
  appSubtitle: 'AD Defense Platform',
  // Localization defaults
  supportedLanguages: ['en', 'fr'],
  defaultLanguage: 'fr'
};

// You can extend this file with environment-specific overrides or
// export helper loaders to fetch persisted configuration.
